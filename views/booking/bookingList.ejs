<div class="min-h-screen flex items-center justify-center bg-gray-900 py-20">
  <div class="w-full mx-20 p-8">
    <% %>
    <h1 class="text-3xl font-bold text-center mb-8 gradient-text">
      🎮 All Bookings
    </h1>

    <% if (bookings.length === 0) { %>
    <p class="text-center text-gray-400">No bookings yet.</p>
    <% } else { %>
    <div class="grid gap-10 md:grid-cols-2">
      <% bookings.forEach(booking => { %>
      <div
        class="bg-gray-800/60 backdrop-blur-lg border border-gray-700/50 p-6 rounded-2xl shadow-lg hover-glow transform transition-all hover:scale-[1.02]"
      >
        <h2 class="text-xl font-semibold text-purple-400 mb-2">
          👤 <%= booking.name %>
        </h2>

        <p>📧 <strong>Email:</strong> <%= booking.email %></p>
        <p>📞 <strong>Phone:</strong> <%= booking.phone %></p>
        <p>👥 <strong>Members:</strong> <%= booking.membersCount %></p>

        <% if (booking.membersName.length > 0) { %>
        <p class="mt-2">
          📝 <strong>Names:</strong> <%= booking.membersName.join(", ") %>
        </p>
        <% } %>

        <p class="mt-2">💻 <strong>Type:</strong> <%= booking.bookingType %></p>
        <p class="mt-2">
          ⏰ <strong>Time:</strong>
          <%= new Date(booking.bookingTime.start).toLocaleString("en-IN", {
          dateStyle: "medium", timeStyle: "short" }) %> → <%= new
          Date(booking.bookingTime.end).toLocaleString("en-IN", { dateStyle:
          "medium", timeStyle: "short" }) %>
        </p>
      </div>
      <% }) %>
    </div>
    <% } %>

    <!-- Buttons -->
    <div class="text-center mt-12">
      <a
        href="/booking"
        class="bg-purple-600 hover:bg-purple-700 px-8 py-3 rounded-xl text-lg font-semibold transition-all transform hover:scale-105 hover-glow"
      >
        ➕ New Booking
      </a>
    </div>

    <div class="text-center mt-6">
      <a
        href="/"
        class="bg-gray-700 hover:bg-gray-600 px-6 py-2 rounded-xl text-white font-semibold transition-all transform hover:scale-105 shadow-md inline-block"
      >
        🏠 Go Home
      </a>
    </div>
  </div>
</div>
