<div class="min-h-screen flex items-center justify-center bg-gray-900 py-20">
  <div
    class="w-full max-w-2xl bg-gray-800 p-8 rounded-2xl shadow-lg border border-gray-700 transition-all transform hover:shadow-purple-500/30 hover:border-purple-500/50"
  >
    <!-- Title -->
    <h1 class="text-3xl font-bold text-center mb-8 text-purple-400">
      🎮 Book Your Gaming Experience
    </h1>

    <!-- Booking Form -->
    <form action="/booking" method="POST" class="space-y-6 text-gray-300">
      <!-- Name -->
      <div>
        <label class="block mb-2 font-semibold text-white">Full Name</label>
        <input
          type="text"
          name="name"
          required
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
        />
      </div>

      <!-- Email -->
      <div>
        <label class="block mb-2 font-semibold text-white">Email</label>
        <input
          type="email"
          name="email"
          required
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
        />
      </div>

      <!-- Phone -->
      <div>
        <label class="block mb-2 font-semibold text-white">Phone Number</label>
        <input
          type="text"
          name="phone"
          pattern="\d{10}"
          required
          placeholder="10-digit number"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
        />
      </div>

      <!-- Booking Type -->
      <div>
        <label class="block mb-2 font-semibold text-white">Booking Type</label>
        <select
          name="bookingType"
          required
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
        >
          <option value="PC">Gaming PC</option>
          <option value="VR">VR Experience</option>
        </select>
      </div>

      <!-- Members Count -->
      <div>
        <label class="block mb-2 font-semibold text-white"
          >Number of Members</label
        >
        <input
          type="number"
          name="membersCount"
          value="1"
          min="1"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
        />
      </div>

      <!-- Members Names -->
      <div>
        <label class="block mb-2 font-semibold text-white">
          Members Names (comma separated)
        </label>
        <input
          type="text"
          name="membersName"
          placeholder="e.g. John, Sarah, Alex"
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
        />
      </div>

      <!-- Booking Time -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <label class="block mb-2 font-semibold text-white">Start Time</label>
          <input
            type="datetime-local"
            name="bookingTime[start]"
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
          />
        </div>
        <div>
          <label class="block mb-2 font-semibold text-white">End Time</label>
          <input
            type="datetime-local"
            name="bookingTime[end]"
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-purple-500"
          />
        </div>
      </div>

      <!-- Submit -->
      <div class="text-center">
        <button
          type="submit"
          class="bg-purple-600 hover:bg-purple-700 px-8 py-3 rounded-lg text-lg font-semibold transition-all transform hover:scale-105 shadow-lg"
        >
          🚀 Confirm Booking
        </button>
      </div>
    </form>

    <!-- Home Button -->
    <div class="text-center mt-6">
      <a
        href="/"
        class="bg-gray-700 hover:bg-gray-600 px-6 py-2 rounded-lg text-white font-semibold transition-all transform hover:scale-105 shadow-md inline-block"
      >
        🏠 Go Home
      </a>
    </div>
  </div>
</div>
